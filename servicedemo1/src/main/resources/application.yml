server:
  port: 9090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    ip-address: localhost
spring:
  application:
    name: user-service
#  cloud:
#    gateway:
#        routes:
#          - id: userModule
#            uri: lb://USER-SERVICE
#            predicates:
#                - Path=/user/**